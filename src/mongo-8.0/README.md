
## Mongo restore
Find the backup file at /appz/backup
Manually execute the restore.sh script
Add the backup file to be restored as argument to the restore.sh script

example:- bash restore.sh <filename>
